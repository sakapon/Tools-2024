<Window x:Class="HanoiTowerWpf201.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HanoiTowerWpf201"
        mc:Ignorable="d"
        Title="Tower of Hanoi" Height="600" Width="900" FontSize="36">
    <Window.Resources>
        <local:ScriptFuncConverter x:Key="DiskWidthConverter" ToFunc="x => x * 30 + 10"/>
        <local:ScriptFuncConverter x:Key="DiskLeftConverter" ToFunc="x => x * 290"/>
        <local:ScriptFuncConverter x:Key="DiskTopConverter" ToFunc="x => (x + 1) * -32"/>
        <ItemsPanelTemplate x:Key="DisksTemplate">
            <Grid/>
        </ItemsPanelTemplate>
        <DataTemplate x:Key="DiskTemplate">
            <Rectangle Width="{Binding Id, Converter={StaticResource DiskWidthConverter}}" Height="30" Fill="#FF3399FF" RadiusX="6" RadiusY="6">
                <Rectangle.RenderTransform>
                    <TransformGroup>
                        <TranslateTransform X="{Binding TowerId.Value, Converter={StaticResource DiskLeftConverter}}" Y="{Binding IndexInTower.Value, Converter={StaticResource DiskTopConverter}}"/>
                    </TransformGroup>
                </Rectangle.RenderTransform>
            </Rectangle>
        </DataTemplate>
    </Window.Resources>
    <Window.DataContext>
        <local:AppModel/>
    </Window.DataContext>
    <Grid>
        <TextBlock Text="{Binding Count.Value}" Margin="50,30" HorizontalAlignment="Right"/>
        <ItemsControl ItemTemplate="{DynamicResource DiskTemplate}" ItemsPanel="{DynamicResource DisksTemplate}" ItemsSource="{Binding Disks}" VerticalAlignment="Top" HorizontalAlignment="Left">
            <ItemsControl.RenderTransform>
                <TransformGroup>
                    <TranslateTransform X="50" Y="360"/>
                </TransformGroup>
            </ItemsControl.RenderTransform>
        </ItemsControl>
    </Grid>
</Window>
