<Window x:Class="KotaniAntViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KotaniAntViewer"
        mc:Ignorable="d"
        Title="Kotani's Ant Viewer" Height="600" Width="900">
    <Window.Resources>
        <DataTemplate x:Key="CellTemplate">
            <Rectangle Fill="{Binding Color}"/>
        </DataTemplate>
        <ItemsPanelTemplate x:Key="CellsTemplate">
            <UniformGrid Rows="{Binding Size}" Columns="{Binding Size}"/>
        </ItemsPanelTemplate>
    </Window.Resources>
    <Window.DataContext>
        <local:AppModel/>
    </Window.DataContext>
    <Grid>
        <ItemsControl ItemsPanel="{DynamicResource CellsTemplate}" ItemTemplate="{DynamicResource CellTemplate}" ItemsSource="{Binding Cells}" Width="500" Height="500"/>
    </Grid>
</Window>
